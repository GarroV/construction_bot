# Construction Bot

> Внутренний инструмент Dodo. Репозиторий приватный: в нём эксплуатационные детали портала
> Битрикс24 и настройки доступа. Канон требований — [`construction-bot-spec.md`](construction-bot-spec.md),
> задачи — в Issues.

## 1. Что это

Construction Bot — телеграм-бот, который несколько раз в день (в 9, 11, 13, 15, 17 и 19 по
местному времени чата) собирает изменения по стройкам пиццерий из
задач Битрикс24 (комментарии, статусы, чек-листы, файлы) и присылает короткую выжимку в топик
странового чата партнёра. Вместо того чтобы владелец сети или партнёр вручную открывали карточку
задачи в Битриксе, они читают одно сообщение в Telegram: что обсуждали за день, какие решения или
блокеры появились, какие файлы приложили.

Бот работает по подписке «топик ↔ карточка»: один топик может отслеживать несколько карточек
(строек), одна карточка может быть подписана в нескольких топиках. Если за сутки вообще ни по
одной карточке чата не было ничего нового — дайджест не приходит совсем (чат не заваливается
пустыми сообщениями). Если изменения есть хотя бы по одной карточке — приходит одно сообщение
на чат: карточки с изменениями получают полный блок с выжимкой, а карточки без изменений за
день — короткую строку «изменений нет» внутри того же сообщения (отдельного дайджеста ради них
не будет). Если молчание тянется дольше недели — раз в неделю приходит короткое напоминание, что
подписка жива. Всё управление — через команды прямо в чате, без отдельной админки.

## 2. Поверхности

### Команды бота

| Команда | Что делает |
|---|---|
| `/add <id или ссылка>` | Подписать текущий топик на карточку Битрикса |
| `/remove <id>` | Снять подписку топика с карточки |
| `/list` | Показать карточки, отслеживаемые в этом топике |
| `/report [id или ссылка]` | Дайджест прямо сейчас, не дожидаясь расписания |
| `/time <ЧЧ:ММ> [город или таймзона]` | Задать локальное время дайджеста для чата |
| `/lang <код>` | Сменить язык дайджеста (`ru`, `en`, …) |
| `/menu` | Панель с кнопками — вся работа тыканьем, без адресных команд |

Ниже — поведение, которое из таблицы не видно.

#### Как указывается карточка

Карточек в Битриксе миллион, поэтому поиска по названию нет. `/add` и `/report` принимают
голый ID (`/add 8017`) либо ссылку, скопированную из браузера — ID вытаскивается регэкспом
из `.../tasks/task/view/<id>/`. Карточка не найдена или нет доступа — бот сообщает об этом.

#### Голая команда открывает диалог

`/add`, `/remove`, `/time`, `/lang` без аргументов не показывают usage-подсказку, а открывают
диалог: reply-промпт или клавиатуру выбора. Подсказка осталась только для непустых невалидных
аргументов — например `/time 9:99` или `/add фигня`.

#### Подзадачи подхватываются сами

Подзадачи отслеживаемой карточки бот подписывает сам на каждом прогоне дайджеста — отдельный
`/add` на них не нужен. В `/list` они помечены «↳» вместо «•», то есть видно, что карточку
завёл бот. `/remove` на ручную карточку снимает и её авто-подхваченные подзадачи; снять
подзадачу отдельно тоже можно.

Повторный `/add` на уже отслеживаемую карточку — no-op с уведомлением, на ранее снятую —
реактивирует подписку.

#### `/report` всегда возвращается с содержимым

Без аргумента (как и кнопка «📊 Отчёт сейчас») команда сначала показывает выбор охвата:
«По всем» — отчёт по всему топику, либо кнопка конкретной карточки — точечный отчёт только по
ней, без дискавери подзадач, поэтому быстрее. В списке кнопок — только **ручные** карточки:
авто-подхваченные подразумеваются родителем и едут вместе с ним.

С аргументом отчёт запускается сразу: по родителю уходят и его подзадачи, по самой подзадаче —
только она. Карточка не отслеживается в топике — бот скажет.

Явный запрос отчёта, в отличие от дневного дайджеста, **не молчит**: карточка без новых
изменений получает не короткую строку, а сводку текущего состояния — последние комментарии плюс
чек-лист, LLM-текст по `prompts/overview.txt`. Только если карточку буквально никогда не
обсуждали, придёт информативная строка с датой последнего дайджеста («…изменений нет
(ДД.ММ ЧЧ:ММ)», либо «с момента подключения»). Тот же текст — последний fallback, если охват
не дал ни одной карточки. Дневное расписание `/report` не ломает.

#### `/time` понимает города

Второй аргумент принимает и человеческие названия («Белград», «Belgrade»), и IANA-идентификатор
(«Europe/Belgrade») — резолвится через `resolve_tz` (`src/telegram/tz_aliases.py`, города и
страны сети Dodo International). При первой настройке таймзону нужно указать явно, если она не
подхватилась автоматически (см. ниже); дальше можно менять только время, сохраняя заданную
таймзону.

#### `/lang` влияет и на LLM

Язык меняется не только у ответов бота, но и у выжимки: LLM пишет дайджест на языке чата.

Все ответы бота локализованы (`locales/ru.json`, `locales/en.json`) и идут `reply` на исходное
сообщение с командой.

**Автоопределение таймзоны при первом контакте.** Telegram не отдаёт таймзону пользователя, но
домен бота ограничен странами сети Dodo International — значит название чата почти всегда и есть
страна/город. При первом создании записи чата (`repo.upsert_chat` возвращает `created=True`) бот
пробует резолвить название супергруппы через `resolve_tz` (`src/telegram/tz_aliases.py`) и, если
получилось, тихо выставляет найденную таймзону (время остаётся дефолтным `09:00`) — без
дополнительного сообщения партнёру, таймзона видна позже через `/time`. Не резолвится (например
служебный чат вроде `group_za_test`) — чат остаётся на дефолтной `UTC`, как и раньше.

**Автонастройка чата из карточки при первом `/add`.** Название чата — грубое приближение (страна
целиком), а вот название карточки Битрикса обычно называет конкретный город стройки, и обсуждение
в ней уже показывает, на каком языке общается команда — этого достаточно, чтобы уточнить и язык, и
таймзону одним LLM-вызовом. Если чат ещё не был настроен вручную (`chats.auto_configured = FALSE`),
после успешного `/add` бот берёт название карточки и до 10 последних комментариев обсуждения и
одним structured-output вызовом (`llm.detect_card_locale`, `src/digest/llm.py`) определяет: **язык**
(`ru`, если ТЕКСТ комментариев обсуждения преимущественно русский, иначе `en` — только эти два
значения; название карточки и имена авторов для языка не учитываются вовсе — оба часто латиницей
даже у русскоязычной команды, например «Podgorica-2» / «Cheredanova Kristina» при обсуждении на
русском) и **IANA-таймзону** города из названия карточки — единственного источника для этого поля
(например «Podgorica-2» → `Europe/Podgorica`; город неясен → ничего). Успешный детект тихо применяет
язык (`/lang`-эквивалент) и, если таймзона резолвилась и прошла валидацию, время `09:00` в ней
(`/time`-эквивалент) — без дополнительных сообщений партнёру, ответ `/add` не меняется.
`auto_configured` становится `TRUE` только при полном успехе детекта и применения — тогда детект
больше не запускается для этого чата; при любой ошибке флаг остаётся `FALSE` (подробности — «Деградация
тихая» ниже). **Ручная настройка побеждает всегда**: `/lang` и
`/time` сами ставят `auto_configured = TRUE`, что сразу и навсегда отключает автонастройку из
карточки. **Деградация тихая**: LLM недоступен после ретраев, вернул мусор, сеть или БД подвели —
`/add` не падает и не тормозит сверх обычного, настройки чата не меняются, флаг остаётся `FALSE` —
бот попробует снова при следующем `/add`.

### Автоматические сообщения

| Событие | Когда приходит |
| Сообщение | Когда приходит |
|---|---|
| Дайджест чата | В слотах `9, 11, 13, 15, 17, 19` по таймзоне чата, если по карточкам чата было хоть что-то новое |
| Недельный пинг | Раз в неделю, если у чата есть активная карточка, но дайджестов давно не было |
| Сводка об ошибках | В личку владельцу (`ADMIN_CHAT_ID`) после прогона с ошибками |

**Дайджест чата.** Слоты задаются `DIGEST_HOURS`. Поводом считаются изменения карточки
(статус, дедлайн, чек-лист), новые комментарии в чате задачи и новые файлы. Блоки всех карточек
чата — включая «изменений нет» и авто-подхваченные подзадачи — склеиваются пустой строкой в
**одно** сообщение. Сплит на несколько сообщений бывает только при превышении лимита длины и
идёт по границам блоков: карточка не рвётся пополам.

**Недельный пинг.** Приходит, если у чата есть хотя бы одна активная карточка, но
`WEEKLY_PING_DAYS` (по умолчанию 7) дней не было ни содержательного дайджеста, ни предыдущего
пинга. Одна строка вида «по стройкам без изменений с {дата}» — подтверждение, что подписка жива.

Про сводку об ошибках — в разделе [«Эксплуатация»](#5-эксплуатация): там же, где остальное,
что нужно знать при дежурстве.

## 3. Сквозной сценарий

1. Партнёр (или админ) в топике страны пишет `/add 8017`.
2. Бот запрашивает у Битрикса задачу `8017` (`tasks.task.get`), сохраняет alias (заголовок задачи),
   запоминает текущие `last_history_id`/`last_message_id` как стартовый курсор и подтверждает
   подписку: «Отслеживаю: <название> (#8017)».
3. Планировщик тикает каждые `SCHEDULER_TICK_MINUTES` минут (по умолчанию 5) и для каждого
   активного чата проверяет, наступил ли очередной слот `DIGEST_HOURS` (9/11/13/15/17/19 по
   таймзоне чата), по которому ещё не было прогона (`last_run_at`). Ночью и до 9:00 — не проверяет;
   слот без изменений и так молчит (LLM не вызывается, сообщение не уходит).
4. Перед сбором дельты бот проверяет подзадачи ручных карточек чата (`tasks.task.list` с
   `PARENT_ID`) и сам подписывает топик на новые — они появляются в `/list` с пометкой «↳» и живут
   своей жизнью дальше (отдельные `/remove` тоже возможны).
5. Когда время подошло: бот забирает у Битрикса историю задачи и сообщения чата задачи начиная с
   сохранённого курсора, собирает «дельту» (что изменилось + что написали + какие файлы прислали —
   новая карточка даёт прямую ссылку на файл, старая — ссылку на комментарий с файлом).
6. Если дельта по карточке не пустая — дельта уходит в LLM (`prompts/digest.txt`) с просьбой сделать
   выжимку на языке чата; при недоступности LLM после ретраев — fallback на сырой список изменений
   с пометкой «краткая версия без выжимки». Так по каждой карточке чата получается свой блок текста
   (карточки без изменений получают короткую строку «изменений нет» — без вызова LLM).
7. Блоки всех карточек чата склеиваются пустой строкой в **одно сообщение** (`html.escape`,
   лимит 4096 символов на сообщение) и отправляются в топик чата; если суммарная длина превышает
   рабочий лимит — сплит на минимальное число сообщений **по границам блоков** (блок карточки не
   рвётся пополам), сообщения уходят последовательно. Только после подтверждённой успешной отправки
   сообщения курсоры **всех карточек, чей блок в него вошёл**, сдвигаются разом — сбой одного
   сообщения (при сплите) не блокирует и не теряет дельту карточек из уже отправленных сообщений.
8. Партнёр читает дайджест в своём топике, ничего не открывая в Битриксе.

## 4. Запуск

### Переменные окружения

Скопировать `.env.example` в `.env` и заполнить (см. актуальный список ключей в самом файле):

```bash
cp .env.example .env
```

Ключевые переменные:

- `BITRIX_WEBHOOK_URL` — входящий вебхук Битрикс24 (`https://<portal>/rest/<user_id>/<token>/`).
- `TELEGRAM_BOT_TOKEN` — токен бота от BotFather.
- `OPENAI_API_KEY` / `OPENAI_MODEL` — доступ к LLM для выжимок (модель по умолчанию `gpt-5.6-terra` — не reasoning-модель: весь бюджет токенов идёт в текст выжимки, а не в скрытые рассуждения).
- `POSTGRES_DSN` — строка подключения к Postgres (в `docker-compose.yml` — `postgres` как хост).
- `SCHEDULER_TICK_MINUTES` — период тика планировщика (по умолчанию 5).
- `DEFAULT_LANGUAGE` — язык нового чата до автоопределения (по умолчанию `en`; при первом `/add` язык уточняется из карточки — русское обсуждение → `ru`).
- `ADMIN_CHAT_ID` — telegram id, куда приходят сводки об ошибках прогона (необязательно).
- `WEEKLY_PING_DAYS` — порог тишины для недельного пинга (по умолчанию 7).
- `DRY_RUN` — `true`/`false`. В режиме `DRY_RUN=true` полный пайплайн (Битрикс + LLM) отрабатывает
  как обычно, но вместо отправки в Telegram текст дайджеста печатается в stdout контейнера — удобно
  для обкатки без риска заспамить реальные чаты.

### Локальный запуск

```bash
docker compose up -d --build
docker compose logs -f bot
```

`docker-compose.yml` поднимает два сервиса: `bot` (само приложение, точка входа — `python -m
src.main`) и `postgres` (с healthcheck и лейблом `backup.pgdump=true` для автобэкапа на
MUSPELHEIM). При старте `src/main.py` сам накатывает недостающие миграции из `migrations/` перед
тем как поднять polling и планировщик — отдельного шага миграции руками не требуется.

### Раскатка на MUSPELHEIM

Бот живёт контейнером на домашнем сервере (`C:\projects\construction_bot`), раскатка — ручная:

```powershell
cd C:\projects\construction_bot
git pull --ff-only
docker compose up -d --build bot
```

**Проверять, что раскатка доехала, обязательно — молчаливый провал уже случался.** 23.09.2026
`git pull` падал на несуществующем SSH-алиасе в `origin` (`git@github-construction:…`, записи
`Host` для него в `~/.ssh/config` сервера нет), но `docker compose up --build` после него
отрабатывал успешно и **пересобирал образ из старого кода** — контейнер выглядел свежим,
фикса в нём не было. Remote переключён на `git@github.com:GarroV/construction_bot.git`.

Две сверки после каждой раскатки — по факту, а не по успешному виду команд:

```powershell
git rev-parse --short HEAD          # должен совпасть с origin/main
docker exec construction_bot-bot-1 grep -c <маркер фикса> /app/src/<файл>
```

### Смоук-скрипт Битрикса

`scripts/smoke_bitrix.py` — разовая проверка вебхука по живой карточке (историю задачи, сообщения
чата задачи, файлы, чек-лист) с сохранением обезличенных фикстур в `tests/fixtures/live/`:

```bash
.venv/bin/python -m scripts.smoke_bitrix <task_id>
```

### Тесты

Юнит-тесты, не требующие БД:

```bash
.venv/bin/pytest -v
```

Тесты, помеченные `db` (например `tests/test_db.py`, `tests/test_repo.py`), требуют переменную
`TEST_POSTGRES_DSN` — она должна указывать на **отдельную** тестовую базу, не на продовую из
`docker-compose.yml`. База поднимается на время прогона и удаляется сама:

```bash
# поднять одноразовую Postgres (исчезнет вместе с данными при остановке)
docker run --rm -d --name cb-test-pg -p 5544:5432 \
  -e POSTGRES_USER=bot -e POSTGRES_PASSWORD=bot -e POSTGRES_DB=botdb postgres:16-alpine

TEST_POSTGRES_DSN=postgresql://bot:bot@localhost:5544/botdb .venv/bin/pytest -v

docker stop cb-test-pg
```

Без `TEST_POSTGRES_DSN` тесты с маркером `db` (32 штуки) пропускаются — зелёный прогон без
переменной **не** подтверждает работоспособность слоя БД.

⚠️ **Из чистого клона прогон сейчас обрывается на сборе.** `tests/test_methods.py` читает
`tests/fixtures/live/checklist.json` на этапе импорта, а живые фикстуры в git не идут (ПДн).
Одна ошибка сбора останавливает весь набор — выполняется 0 тестов из 324. Пока не исправлено
(issue #13), прогон делается в обход:

```bash
.venv/bin/pytest -q --ignore=tests/test_methods.py   # 285 passed, 32 skipped
```

Проверено 2026-08-26 на Python 3.12.

## 5. Эксплуатация

- **Сводки об ошибках.** Если `ADMIN_CHAT_ID` задан (это **личка владельца**, не страновой чат —
  партнёры сводок не видят), после тика с ошибками в него приходит сообщение с проблемами за
  прогон. Ошибки **сгруппированы по категориям** (🤖 LLM / 📋 Битрикс / ⚠️ Прочее), чтобы сразу
  видеть суть. Отдельно обрабатываются **транзиентные сетевые обрывы** — канала сервера и к
  Telegram, и к Битриксу (`TelegramNetworkError` / `ClientConnectorError` / `TRANSPORT_ERROR: All
  connection attempts failed` — внешняя проблема, не баг: бот ретраит, дельта не теряется): их
  сырой стектрейс наружу **не идёт** и не спамит каждым прогоном (плохой канал = нон-стоп поток) —
  вместо этого один понятный **суточный итог** («N обращений сорвались из-за нестабильной сети»).
  Пустой прогон без ошибок сводку не шлёт.
- **Ограничение доступа к командам (`restricted`).** Флаг `chats.restricted` по умолчанию `FALSE`
  (команды доступны всем в чате). Если по конкретной стране понадобится ограничить управление
  списком отслеживаемых карточек, флаг переключается и заполняется `chat_admins` — **руками в БД**,
  кода это не касается (сознательное решение MVP — команда управления вайтлистом появится только
  если ограничение реально понадобится):

  ```sql
  UPDATE chats SET restricted = TRUE WHERE id = <chat_id>;
  INSERT INTO chat_admins (chat_id, telegram_user_id) VALUES (<chat_id>, <telegram_user_id>);
  ```

- **Один polling-инстанс.** aiogram long polling не терпит двух одновременно запущенных процессов
  на одном токене — перед стартом на новом окружении (например при переезде с локального
  DRY_RUN-контейнера на MUSPELHEIM) предыдущий инстанс нужно останавливать (`docker compose down`).
- **Ручной сброс дайджеста для теста** — если нужно прогнать дайджест повторно в тот же
  календарный день, не дожидаясь следующих суток: `UPDATE chats SET last_digest_date = NULL WHERE
  id = <chat_id>;`.
- **Ручной сброс автонастройки чата (`chats.auto_configured`, миграция 0005).** Если нужно снова
  прогнать LLM-детект языка/таймзоны из карточки на следующем `/add` (например для теста, или чат
  раньше получил детект с ошибочным результатом) — руками в БД: `UPDATE chats SET auto_configured =
  FALSE WHERE id = <chat_id>;`. Обратная операция (`= TRUE`) навсегда отключает автонастройку для
  чата, не трогая ручной `/lang`/`/time` — тот же эффект, что и от самих этих команд.
- **Битая `chats.timezone` не глушит бота.** `chats.timezone` пишется только через `/time`
  (валидируется `resolve_tz`), но ручная правка в БД — легальный сценарий (как у `restricted`
  выше) и теоретически может занести невалидное значение. `src/digest/scheduler.safe_zoneinfo`
  оборачивает каждое обращение к `chats.timezone` (тик, `/report`/кнопки, `force_report.py`):
  невалидная таймзона не роняет прогон исключением `ZoneInfoNotFoundError`, а деградирует на UTC
  с `WARNING` в лог (сигнал не теряется молча).
- **Кэш LLM-выжимок (`llm_cache`, миграция 0004, TTL 24h).** Повторные «Отчёт сейчас» по
  неизменившимся данным не дёргают OpenAI повторно — выжимка берётся из кэша по хэшу финального
  промпта (`src/digest/llm.material_hash`, `scheduler._summarize_cached`). Тот же кэш спасает
  и от простоя провайдера: если по идентичному промпту уже был успешный вызов, повторный сбой
  OpenAI (например 500) отдаёт готовый текст вместо fallback-версии «без выжимки». Bitrix
  при этом дёргается на каждом прогоне как обычно — он и остаётся источником инвалидации:
  изменилось хоть что-то в сырье (комментарий, статус, чек-лист) — хэш другой, кэш промахивается.
  Сбой самого кэша (БД недоступна) не ломает генерацию — бот просто работает без кэша с
  `WARNING` в лог. Ручной сброс не нужен: `TRUNCATE llm_cache;` при необходимости всё же
  почистить кэш руками.

### Интеграция с Битрикс24

Поведение конкретного портала — права вебхука, чтение комментариев у карточек старого типа,
недоступность `disk.file.get`, правила антибота — вынесено в
[docs/bitrix-integration.md](docs/bitrix-integration.md). Там же команда перепроверки живьём.

## 6. Канон требований

Полная спецификация продукта и всех технических решений — `construction-bot-spec.md` в корне
репозитория. План разработки по фазам и задачам — `docs/plans/2026-07-21-mvp-phases-0-2.md`.
Этот README описывает продукт по факту реализованного кода; при расхождении со спекой
приоритет — у спеки как источника требований, а расхождение — повод завести issue.
